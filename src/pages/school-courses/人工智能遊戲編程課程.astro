---
import Layout from '../../layouts/Layout.astro';
import HeroSection from '../../components/course/HeroSection.astro';
import AccordionSection from '../../components/course/AccordionSection.astro';
import ContactForm from '../../components/homepage/ContactForm.astro';
import { Image } from 'astro:assets';

// Course data (static)
const courseData = {
  title: "人工智能遊戲編程課程",
  subtitle: "三小時製作兩個遊戲",
  tags: ["Programming 編程", "AI 人工智能", "初中", "高中", "遊戲開發", "網頁遊戲"],
  videoId: "dQw4w9WgXcQ", // Placeholder video ID
  description: "本課程將教授學生如何使用人工智能輔助開發網頁遊戲。學生將學習使用 HTML、CSS 和 JavaScript 製作網頁遊戲，並透過 Poe 和 Repl.it 等 AI 工具簡化開發流程。網頁遊戲相比其他平台遊戲更容易開發，無需複雜的安裝程序，學生可以快速看到成果並分享給朋友。就算是零基礎的學生，只要想做，就可以製作屬於自己的遊戲！"
};

// Course curriculum
const curriculumItems = [
  {
    title: "AI Coding 入門",
    content: [
      "認識不同的客戶端執行環境（手機遊戲, 電腦遊戲, 網頁遊戲）",
      "認識反覆設計流程 (設計 - 實作 - 評估)",
      "使用 Poe 平台協助設計井字棋原型",
      "使用 Repl.it 環境建構井字棋初步成品",
      "測試系統與除錯"
    ]
  },
  {
    title: "加入難度選擇",
    content: [
      "增加 4x4 棋盤模式",
      "測試系統與除錯"
    ]
  },
  {
    title: "客製化成品",
    content: [
      "新增自選功能",
      "測試系統與除錯"
    ]
  },
  {
    title: "輸出專案至互聯網",
    content: [
      "透過 Repl.it 將專案輸出至互聯網",
      "與同學交流各自井字棋成品"
    ]
  },
  {
    title: "專案式學習",
    content: [
      "簡述專案式學習內容",
      "與Poe 平台協作，設計自選遊戲原型",
      "使用 Repl.it 製作自選遊戲成品",
      "測試系統與除錯",
      "與同學交流各自成品",
      "分享使用人工智能編程的感想"
    ]
  }
];

// Course highlights
const courseHighlights = [
  {
    icon: "fas fa-robot",
    title: "AI 編程入門",
    description: "學習使用 Poe 和 Repl.it 等 AI 工具進行遊戲開發，無需深厚編程基礎"
  },
  {
    icon: "fas fa-gamepad",
    title: "網頁遊戲開發實戰",
    description: "使用 HTML、CSS、JavaScript 從井字棋開始，逐步製作踩地雷、食鬼、數獨等經典網頁遊戲"
  },
  {
    icon: "fas fa-code",
    title: "網頁遊戲優勢",
    description: "網頁遊戲無需安裝，可在任何設備上運行，開發簡單且易於分享"
  },
  {
    icon: "fas fa-users",
    title: "協作學習",
    description: "與同學交流作品，分享開發心得，建立編程社群"
  },
  {
    icon: "fas fa-globe",
    title: "線上部署",
    description: "學習將作品部署到互聯網，讓全世界都能玩到你的遊戲"
  }
];

// Course requirements
const courseRequirements = [
  {
    icon: "fas fa-clock",
    title: "課時",
    description: "6 - 8 小時"
  },
  {
    icon: "fas fa-laptop",
    title: "所需設備",
    description: "手提電腦"
  },
  {
    icon: "fas fa-user-graduate",
    title: "學員要求",
    description: "中一至中六"
  },
  {
    icon: "fas fa-sign-in-alt",
    title: "帳戶需求",
    description: "Repl.it 帳戶"
  }
];

// FAQ items
const faqItems = [
  {
    question: "零基礎學生可以參加嗎？",
    answer: "完全可以！本課程專為零基礎學生設計，使用 AI 工具讓編程變得簡單易懂。"
  },
  {
    question: "需要自備電腦嗎？",
    answer: "是的，學生需要自備手提電腦，並確保有穩定的網絡連接。"
  },
  {
    question: "會學到什麼編程語言？",
    answer: "主要使用 HTML、CSS 和 JavaScript 開發網頁遊戲，並學習如何使用 AI 工具輔助編程。"
  },
  {
    question: "課程結束後能做出什麼作品？",
    answer: "學生將完成井字棋遊戲和一個自選遊戲項目，並能將作品部署到互聯網。"
  }
];

// Contact information
const contactInfo = {
  address: "香港九龍灣宏光道1號",
  phone: "+852 1234 5678",
  email: "info@10botics.com",
  hours: "週一至週五 9:00-18:00"
};
---

<Layout title="人工智能遊戲編程課程">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  
  <HeroSection 
    title={courseData.title}
    subtitle={courseData.subtitle}
    tags={courseData.tags}
    heroImage="/images/school-courses/人工智能遊戲編程課程/ai-game-coding-hero.jpg"
    description={courseData.description}
  />

  <!-- Hero image and description section -->
  <section class="bg-white py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <div class="bg-gradient-to-br from-cyan-500 to-cyan-600 rounded-lg p-8 text-white">
            <h3 class="text-2xl font-bold mb-4">AI 編程新時代</h3>
            <p class="text-lg mb-6">
              透過人工智能工具，讓網頁遊戲開發變得前所未有的簡單。學生將學習如何使用 AI 助手設計遊戲邏輯，並使用 HTML、CSS、JavaScript 在實際開發環境中實現創意。
            </p>
            <div class="flex items-center space-x-4">
              <div class="flex items-center">
                <i class="fas fa-robot text-2xl mr-2"></i>
                <span>AI 輔助設計</span>
              </div>
              <div class="flex items-center">
                <i class="fas fa-code text-2xl mr-2"></i>
                <span>實戰編程</span>
              </div>
            </div>
          </div>
        </div>
        <div>
          <h2 class="text-3xl font-bold text-gray-900 mb-6">為什麼選擇 AI 編程？</h2>
          <p class="text-lg text-gray-700 leading-relaxed mb-6">
            在人工智能快速發展的時代，學會與 AI 協作進行網頁遊戲開發是未來的重要技能。本課程讓學生體驗 AI 輔助開發的樂趣，同時建立紮實的網頁編程思維。網頁遊戲開發相比其他平台更簡單，學生可以快速看到成果並分享給朋友。
          </p>
          <div class="space-y-4">
            <div class="flex items-start space-x-3">
              <i class="fas fa-check-circle text-cyan-500 mt-1"></i>
              <span class="text-gray-700">降低編程門檻，專注創意實現</span>
            </div>
            <div class="flex items-start space-x-3">
              <i class="fas fa-check-circle text-cyan-500 mt-1"></i>
              <span class="text-gray-700">學習未來工作模式</span>
            </div>
            <div class="flex items-start space-x-3">
              <i class="fas fa-check-circle text-cyan-500 mt-1"></i>
              <span class="text-gray-700">快速原型開發能力</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Why the technology is important section -->
  <section class="bg-gray-50 py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">為什麼選用這些技術？</h2>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow">
          <div class="flex items-center mb-4">
            <i class="fas fa-brain text-3xl text-cyan-500 mr-4"></i>
            <h3 class="text-xl font-semibold text-gray-900">Poe 平台</h3>
          </div>
          <p class="text-gray-700">
            Poe 提供強大的 AI 助手，能幫助學生設計遊戲邏輯、生成代碼片段，讓編程過程更加直觀和高效。
          </p>
        </div>
        
        <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow">
          <div class="flex items-center mb-4">
            <i class="fas fa-cloud text-3xl text-cyan-500 mr-4"></i>
            <h3 class="text-xl font-semibold text-gray-900">Repl.it 環境</h3>
          </div>
          <p class="text-gray-700">
            Repl.it 提供完整的雲端開發環境，無需本地安裝，支持多種編程語言，讓學生專注於創意實現。
          </p>
        </div>
        
        <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow">
          <div class="flex items-center mb-4">
            <i class="fas fa-gamepad text-3xl text-cyan-500 mr-4"></i>
            <h3 class="text-xl font-semibold text-gray-900">網頁遊戲開發</h3>
          </div>
          <p class="text-gray-700">
            網頁遊戲相比其他平台遊戲更容易開發，無需複雜的安裝程序，學生可以快速看到成果並分享給朋友。通過遊戲開發學習編程，讓抽象的編程概念變得具體有趣。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Course requirements section -->
  <section class="bg-white py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">課程要求</h2>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {courseRequirements.map((requirement) => (
          <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow text-center">
            <div class="flex justify-center mb-4">
              <i class={`${requirement.icon} text-3xl text-cyan-500`}></i>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">{requirement.title}</h3>
            <p class="text-gray-700">{requirement.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Course highlights section -->
  <section class="bg-gray-50 py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">課程重點</h2>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {courseHighlights.map((highlight) => (
          <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow">
            <div class="flex items-center mb-4">
              <i class={`${highlight.icon} text-2xl text-cyan-500 mr-3`}></i>
              <h3 class="text-lg font-semibold text-gray-900">{highlight.title}</h3>
            </div>
            <p class="text-gray-700">{highlight.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Course content/curriculum section -->
  <section class="bg-white py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">課程內容</h2>
      <p class="text-lg text-gray-700 text-center mb-8">共 6-8 小時</p>
      
      <div class="max-w-4xl mx-auto">
        <div class="space-y-4">
          {curriculumItems.map((item, index) => (
            <div class="accordion-item border border-gray-200 rounded-lg overflow-hidden">
              <button 
                class="accordion-header w-full text-left p-6 bg-gray-50 hover:bg-gray-100 transition-colors flex justify-between items-center"
                data-target={`accordion-${index}`}
              >
                <span class="font-semibold text-gray-900">{item.title}</span>
                <div class="accordion-icon transition-transform duration-200">
                  <i class="fas fa-plus text-gray-600"></i>
                </div>
              </button>
              <div 
                id={`accordion-${index}`}
                class="accordion-content max-h-0 overflow-hidden transition-all duration-300 ease-in-out"
              >
                <div class="p-6 bg-white border-t border-gray-200">
                  <ul class="space-y-3">
                    {item.content.map((point) => (
                      <li class="flex items-start space-x-3">
                        <div class="text-cyan-500 mt-1">
                          <i class="fas fa-chevron-right text-sm"></i>
                        </div>
                        <span class="text-gray-700">{point}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ section -->
  <section class="bg-gray-50 py-16">
    <div class="container mx-auto px-4 max-w-4xl">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">常見問題</h2>
      
      <div class="space-y-6">
        {faqItems.map((faq, index) => (
          <div class="bg-white border border-gray-200 rounded-lg overflow-hidden">
            <button 
              class="faq-header w-full text-left p-6 bg-white hover:bg-gray-50 transition-colors flex justify-between items-center"
              data-target={`faq-${index}`}
            >
              <span class="font-semibold text-gray-900">{faq.question}</span>
              <div class="faq-icon transition-transform duration-200">
                <i class="fas fa-plus text-gray-600"></i>
              </div>
            </button>
            <div 
              id={`faq-${index}`}
              class="faq-content max-h-0 overflow-hidden transition-all duration-300 ease-in-out"
            >
              <div class="p-6 bg-gray-50 border-t border-gray-200">
                <p class="text-gray-700">{faq.answer}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Contact section -->
  <ContactForm 
    title="聯絡我們"
    description="想了解更多關於人工智能網頁遊戲編程工作坊的資訊？歡迎聯絡我們，我們會為您提供詳細的課程安排和報名資訊。"
    contactInfo={contactInfo}
  />
</Layout>

<script>
  // Accordion functionality for course content
  document.addEventListener('DOMContentLoaded', function() {
    const headers = document.querySelectorAll('.accordion-header');
    
    headers.forEach(header => {
      header.addEventListener('click', function(this: HTMLElement) {
        const target = this.getAttribute('data-target');
        if (!target) return;
        
        const content = document.getElementById(target);
        const icon = this.querySelector('.accordion-icon i') as HTMLElement;
        
        if (content && icon) {
          // Toggle current accordion
          if (content.style.maxHeight && content.style.maxHeight !== '0px') {
            content.style.maxHeight = '0px';
            icon.className = 'fas fa-plus text-gray-600';
          } else {
            content.style.maxHeight = content.scrollHeight + 'px';
            icon.className = 'fas fa-minus text-gray-600';
          }
        }
      });
    });

    // FAQ functionality
    const faqHeaders = document.querySelectorAll('.faq-header');
    
    faqHeaders.forEach(header => {
      header.addEventListener('click', function(this: HTMLElement) {
        const target = this.getAttribute('data-target');
        if (!target) return;
        
        const content = document.getElementById(target);
        const icon = this.querySelector('.faq-icon i') as HTMLElement;
        
        if (content && icon) {
          // Toggle current FAQ
          if (content.style.maxHeight && content.style.maxHeight !== '0px') {
            content.style.maxHeight = '0px';
            icon.className = 'fas fa-plus text-gray-600';
          } else {
            content.style.maxHeight = content.scrollHeight + 'px';
            icon.className = 'fas fa-minus text-gray-600';
          }
        }
      });
    });
  });
</script>
