---
import Layout from '../../layouts/Layout.astro';
import HeroSection from '../../components/course/HeroSection.astro';
import QnASection from '../../components/course/QnASection.astro';
import SingleCourseContent from '../../components/course/SingleCourseContent.astro';
import { Image } from 'astro:assets';
import ContactSection from '../../components/ContactSection.astro';

import heroImage from '../../assets/images/stemday/mainpage/microbit-car.png';

const activityData: {
  title: string;
  subtitle: string;
  tags: string[];
  description: string;
  heroImageAsset: any;
} = {
  title: "Micro:bit 足球車競賽",
  subtitle: "在Micro:bit 足球車競賽中，學生親身體驗以識別圖像、手勢或平板程式等方式控制模型車。我們希望透過活動，讓同學們有機會體驗到此新穎的遊戲體驗。",
  tags: ["Micro:bit", "編程", "小一至中六", "初小", "高小", "初中", "高中"],
  description: "Micro:bit 足球車競賽讓學生親身體驗以識別圖像、手勢或平板程式等方式控制Tinybit模型車進行足球比賽，體驗新穎的STEM遊戲。",
  heroImageAsset: heroImage
};

const activityFeatures = [
  {
    icon: "fas fa-futbol",
    title: "足球車對戰",
    description: "同學以小組形式進行足球車對戰，透過控制Tinybit小車在足球場上進行刺激的比賽。"
  },
  {
    icon: "fas fa-hand-paper",
    title: "多種操控方式",
    description: "支援圖像識別、手勢控制或平板程式等多種操控方式，讓同學體驗不同的控制技術。"
  },
  {
    icon: "fas fa-microchip",
    title: "Micro:bit 編程",
    description: "活動結合Micro:bit編程技術，讓同學在遊戲中學習基礎編程概念和硬件控制原理。"
  },
  {
    icon: "fas fa-users",
    title: "團隊協作",
    description: "每次可供2至6人同時進行活動，培養同學的團隊合作精神和競技意識。"
  }
];

const activitySchedule = [
  {
    title: "活動簡介",
    content: [
      "導師介紹Micro:bit足球車的基本概念和操控方式",
      "說明比賽規則及操控方法"
    ]
  },
  {
    title: "足球車競賽體驗",
    content: [
      "每次可同時供2-6人進行活動",
      "每次每人可玩2-3分鐘",
      "透過平板電腦或手勢控制Tinybit小車進行足球比賽",
      "導師從旁指導同學掌握操控技巧"
    ]
  }
];

const activityInfo = [
  { label: "容納人數", value: "每次可同時供2-6人進行活動" },
  { label: "時長", value: "每次每人可玩2-3分鐘" },
  { label: "我們將會提供", value: "Tinybit 小車 / 龍門 / 足球 / 足球場背景 / 遊戲介紹易拉架" },
  { label: "學校需提供/自備", value: "Microbit / 桌子 / 平板電腦 / 電腦" },
  { label: "備註", value: "另設常規課程" },
];

const faqData = [
  {
    question: "這項活動適合什麼年級的同學參與？",
    answer: "這項活動適合小一至中六學生參與。"
  },
  {
    question: "每次活動可以容納多少人？",
    answer: "每次可同時供2至6人進行活動，每人每次可玩2至3分鐘。"
  },
  {
    question: "學校需要準備什麼？",
    answer: "學校需要提供Microbit、桌子、平板電腦或電腦。我們將提供Tinybit小車、龍門、足球、足球場背景及遊戲介紹易拉架。\n\n建議先致電+852 6554-1506 或電郵至 info@10botics.com 向同事溝通學校期望的活動內容及安排。"
  },
  {
    question: "有哪些操控方式？",
    answer: "活動支援多種操控方式，包括圖像識別、手勢控制以及平板程式控制，讓同學體驗不同的科技控制技術。"
  }
];

const { title, subtitle, tags, description, heroImageAsset: heroImg } = activityData;
---

<Layout title={title} description={description}>
  <main>
    <HeroSection 
      title={title}
      subtitle={subtitle}
      tags={tags}
      description={description}
      heroImage={heroImg}
    />

    <section class="py-16 bg-gray-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-4xl font-bold mb-4">活動特色</h2>
        </div>
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
          {activityFeatures.map((feature) => (
            <div class="text-center">
              <div class="w-16 h-16 bg-blue-500 rounded-full flex items-center justify-center mx-auto mb-4">
                <i class={`${feature.icon} text-white text-2xl`}></i>
              </div>
              <h3 class="text-xl font-semibold mb-2">{feature.title}</h3>
              <p class="text-gray-600">
                {feature.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <SingleCourseContent 
      items={activitySchedule}
      hours="每人每次2-3分鐘"
    />

    <section class="py-16 bg-gray-50">
      <div class="container mx-auto px-4">
        <div class="max-w-3xl mx-auto">
          <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">活動資訊</h2>
          <div class="overflow-hidden rounded-lg shadow-lg border border-gray-200">
            <table class="w-full">
              <tbody>
                {activityInfo.map((item, i) => (
                  <tr class={i % 2 === 0 ? "bg-white" : "bg-gray-50"}>
                    <td class="px-6 py-4 font-semibold text-gray-900 border-r border-gray-200 w-1/3 text-center">{item.label}</td>
                    <td class="px-6 py-4 text-gray-700 text-center">{item.value}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </section>

    <QnASection faqs={faqData} />

    <ContactSection subtitle="歡迎聯絡我們，我們會為您提供詳細的活動安排和報名資訊。" />
  </main>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('.accordion-header').forEach(button => {
        button.addEventListener('click', () => {
          const item = button.parentElement;
          if (!item) return;
          const isActive = item.classList.contains('active');
          document.querySelectorAll('.accordion-item').forEach(accordionItem => {
            accordionItem.classList.remove('active');
          });
          if (!isActive) {
            item.classList.add('active');
          }
        });
      });
    });
  </script>

  <style>
    .accordion-content {
      display: none;
    }
    .accordion-item.active .accordion-content {
      display: block;
    }
    .accordion-item.active .accordion-header i {
      transform: rotate(45deg);
    }
  </style>
</Layout>
