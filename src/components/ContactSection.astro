---
import PaperformEmbed from './PaperformEmbed.astro';

export interface Props {
  subtitle?: string;
}

const { subtitle = "想了解更多資訊？歡迎聯絡我們，我們會為您提供詳細的課程安排和報名資訊。" } = Astro.props;
---

<section class="bg-gray-50 py-16">
  <div class="container mx-auto px-4 max-w-6xl">
    <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">聯絡我們</h2>
    <div class="bg-white rounded-lg p-8 shadow-lg">
      <div class="text-center">
        <p class="text-lg text-gray-700 mb-6">{subtitle}</p>
        <PaperformEmbed />
      </div>
    </div>
  </div>
</section> 