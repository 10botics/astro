---
// Stats Section based on crawled 10botics.com data
// This was originally Elementor counter widgets showing achievements
---

<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        我們的成就
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        多年來與香港多間學校和教育機構合作，致力推動 STEM 教育發展
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Schools Partnered -->
      <div class="text-center">
        <div class="w-20 h-20 bg-blue-600 rounded-full flex items-center justify-center mb-4 mx-auto">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
          </svg>
        </div>
        <div class="text-4xl md:text-5xl font-bold text-blue-600 mb-2" data-counter="50">
          50+
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">合作學校</h3>
        <p class="text-gray-600">與香港多間學校建立長期合作關係</p>
      </div>
      
      <!-- Students Reached -->
      <div class="text-center">
        <div class="w-20 h-20 bg-green-600 rounded-full flex items-center justify-center mb-4 mx-auto">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z" />
          </svg>
        </div>
        <div class="text-4xl md:text-5xl font-bold text-green-600 mb-2" data-counter="10000">
          10,000+
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">學生受益</h3>
        <p class="text-gray-600">超過一萬名學生參與我們的 STEM 課程</p>
      </div>
      
      <!-- Years Experience -->
      <div class="text-center">
        <div class="w-20 h-20 bg-purple-600 rounded-full flex items-center justify-center mb-4 mx-auto">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div class="text-4xl md:text-5xl font-bold text-purple-600 mb-2" data-counter="5">
          5+
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">年經驗</h3>
        <p class="text-gray-600">專注 STEM 教育研發及教學多年</p>
      </div>
    </div>
    
    <!-- Partner Schools Grid -->
    <div class="mt-16">
      <h3 class="text-2xl font-bold text-gray-900 text-center mb-8">
        合作夥伴學校
      </h3>
      <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-6">
        <!-- Partner School Logos -->
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">保良局蔡繼有學校</span>
          </div>
        </div>
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">聖類斯中學</span>
          </div>
        </div>
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">九龍華仁書院</span>
          </div>
        </div>
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">英皇書院</span>
          </div>
        </div>
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">香港中文大學</span>
          </div>
        </div>
        <div class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
          <div class="w-full h-16 bg-gray-200 rounded flex items-center justify-center">
            <span class="text-gray-500 text-xs text-center">香港資優教育學苑</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Simple counter animation
  const counters = document.querySelectorAll('[data-counter]');
  
  const animateCounter = (element) => {
    const target = parseInt(element.getAttribute('data-counter'));
    const duration = 2000; // 2 seconds
    const step = target / (duration / 16); // 60fps
    let current = 0;
    
    const timer = setInterval(() => {
      current += step;
      if (current >= target) {
        current = target;
        clearInterval(timer);
      }
      element.textContent = Math.floor(current) + (element.textContent.includes('+') ? '+' : '');
    }, 16);
  };
  
  // Intersection Observer to trigger animation when visible
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateCounter(entry.target);
        observer.unobserve(entry.target);
      }
    });
  });
  
  counters.forEach(counter => observer.observe(counter));
</script> 